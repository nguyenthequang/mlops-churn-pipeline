# Minimal runtime deps for the inference API. Pinned to the versions the model
# was trained with so the pickled pipeline loads cleanly in production.
scikit-learn==1.9.0
pandas==2.3.3
joblib==1.5.3
fastapi==0.138.0
uvicorn==0.49.0
